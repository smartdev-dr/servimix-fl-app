import 'package:flutter/material.dart';

class ServiceScreenNext extends StatelessWidget {
  const ServiceScreenNext({super.key});

  @override
  Widget build(BuildContext context) {
    return AppBar(
      title: const Text('service screen next'),
    );
  }
}
